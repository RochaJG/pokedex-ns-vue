// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.iatom.pokedex"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
